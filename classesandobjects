//getters 
#include <bits/stdc++.h>
using namespace std;

class A{
    private :
    int age;
    public:
    A(int x){
        age= x;
    }
    int getData(){
        return age;
    }
};
int main(){
    A a_obj(28);
    cout<< a_obj.getData();
}
